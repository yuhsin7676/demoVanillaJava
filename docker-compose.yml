version: '3.7'

services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: demoVanillaJava_db
    ports:
      - "5432:5432"